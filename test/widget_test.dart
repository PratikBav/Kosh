import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Kosh app smoke test', (WidgetTester tester) async {
    // Placeholder — will be implemented with proper test setup
    // that initializes Isar and ProviderScope.
    expect(true, isTrue);
  });
}
